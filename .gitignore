.venv/
__pycache__/
*.pyc
data/*.db*
config.json
frontend/node_modules/
frontend/dist/
.pytest_cache/

# Real content packs stay out of the repo; only the docs, schema, and example pack are tracked.
content/*
!content/schema.json
!content/SCHEMA.md
!content/example/

# Checks against a private content pack; the pack itself is not in the repo either.
tests/private/
